# Fix the Broken Form: TODO Steps

## Approved Plan Steps:

1. [x] Create BUG_REPORT.md documenting each bug's root cause and symptom.
2. [x] Edit src/App.jsx: Implement proper validate() function.
3. [x] Edit src/App.jsx: Fix handleSubmit (validation gate, loading, reset, error handling).
4. [x] Edit src/App.jsx: Update handleChange to clear field errors.
5. [x] Edit src/App.jsx: Wire errors/loading to JSX (styles, messages, button state).
6. [x] Test all 6 bugs fixed (run `npm install && npm run dev`, test in browser: empty submit blocked + field errors; double submit disabled; success resets form; "login" title → field error; stepsCount invalid rejected).
7. [ ] attempt_completion with results and demo command.
